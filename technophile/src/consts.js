import { Vector3 } from 'three';

// 3D CONSTANTS:
export const SIZE = 100;
export const ORIGIN = new Vector3(0, 0, 0);
export const CENTER = new Vector3(SIZE / 2, SIZE / 2, SIZE / 2);

// Indices in data cols:
export const X = 2;
export const Y = 3;
export const Z = 1;
export const COLOR = 0;
